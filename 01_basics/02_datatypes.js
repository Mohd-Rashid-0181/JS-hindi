"use strict"; // treat all JS code as newer version 

// alert (3 + 3)  // we are using node.js not browser this syntax is valid for browser

console.log(3
    +
    3); // code readability should be high 

    console.log("hitesh"); // you should try to write the code in that way so that anyone should read this easily 
    
let name = "rashid"
let age = 18
let isLoggedIn = false

// number => its range is  2 ^53 
// bigint = large number (123n)
// string = "",'' prefer to write it in double code 
// boolean = false/ true 
// undefined = 
// symbol = unique
// null = standalone value means nothing 


console.log(typeof age);  /// typeof  is used for knowning the type of variable 

// the type of null is object 
// the type  of undefined is undefined like string and many more (its special )
